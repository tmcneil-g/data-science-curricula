library("ggplot2")

# Read in data
reproducible.means.scores <- read.csv(file = "data/reproducible_means.csv")

ggplot(data = reproducible.means.scores, mapping = aes(x = Score, y = Subarea)) +
  geom_point(mapping = aes(x = Score, y = Subarea),
             shape = 21,
             fill = "black",
             size = 2.5) + theme_bw()

