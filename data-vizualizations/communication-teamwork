# Generate caterpillar-style plot of sub-area scores for NASEM Area "Communication and teamwork" 

library("ggplot2")

# Read in data
communication_scores <- read.csv(file = "data/communication_long.csv")

# Update description with linebreaks to conserve horizontal space in plot
communication_scores$Subarea <- stringr::str_wrap(string = communication_scores$Subarea, 
                                                      width = 20)

communication_catplot <- ggplot(data = communication_scores, mapping = aes(y = Subarea)) +
  geom_errorbar(mapping = aes(xmin = Mean_Score - (2 * Se_Score),
                              xmax = Mean_Score + (2 * Se_Score)),
                width = 0.0) +
  geom_point(mapping = aes(x = Score, y = Subarea),
             shape = 21,
             fill = "black",
             alpha = 0.25,
             size = 1) + 
  geom_point(mapping = aes(x = Mean_Score, y = Subarea),
             shape = 21,
             fill = "white",
             size = 2.5) + 
  theme_bw() +
  labs(x = "Score",
       y = "Sub-Area")

ggsave(filename = "output/communication.png", 
       plot = communication_catplot,
       width = 10,
       height = 8,
       units = "cm")
